# Copiar a /etc/nginx/conf.d/websocket-map.conf
# El WebSocket de Streamlit (_stcore/stream) necesita reenviar Upgrade/Connection.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
